{
  "name": "doom-reactive-state",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "test": "jest --clearCache && jest --showConfig && jest --verbose --coverage",
    "dev": "cd dev && npm start",
    "release": "npm run build && ./publishing/publish.sh $1"
  },
  "author": "Alessio Coser",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "@types/jsdom": "^21.1.0",
    "jest": "^29.4.2",
    "jest-environment-jsdom": "^29.4.2",
    "jsdom": "^21.1.0",
    "ts-jest": "^29.0.5",
    "typescript": "^4.9.5"
  }
}
